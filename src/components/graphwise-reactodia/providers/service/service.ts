/**
 * Marker interface identifying a class as a singleton service. Services are created on
 * demand and shared through the {@link ServiceProvider} and injected with {@link service}.
 */
export interface Service {}
